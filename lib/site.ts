// Canonical public origin — used for metadataBase, sitemap, robots, and
// structured data. www is the canonical host (see decision-log 2026-06).
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.proscene.app";
