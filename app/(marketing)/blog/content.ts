// Blog index page body (static fallback). Generated from the static posts
// registry in ./posts so the featured card + grid always match the real posts.
import { renderBlogIndexHtml } from "./posts";

export const BLOG_HTML = renderBlogIndexHtml();
